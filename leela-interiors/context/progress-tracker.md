# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

* Feature 15: Item Master unit of measure — completed. All specced work through Feature 15 is shipped.

## Current Goal

* None — all known work complete. Next: reconcile these entries into numbered feature-spec docs if desired (GST Report, delete flows, edit-costing, and unit-of-measure were built without specs and are documented here retroactively).

## Completed

* Feature 15 — Item Master unit of measure (undocumented at implementation time — no feature spec written; reconstructed from code + live DB on 2026-07-09): migration `20260709031711_add_item_unit.sql` adds `unit text NOT NULL DEFAULT 'nos'` to `item_master`, applied to the live Supabase project. `lib/constants.ts` adds `UNITS = ['nos','sqft','rft','set','lot']`, `UNIT_LABELS`, `DEFAULT_UNIT`. `app/(app)/items/ItemsManager.tsx` gained a Unit column/select in both the add-item row and inline edit row. `app/api/items/route.ts` and `app/api/items/[id]/route.ts` accept/persist `unit`. `components/costing/ItemRow.tsx` shows a per-line Unit select (fills from master item on selection, defaults to `nos`), `components/costing/ItemSection.tsx`/`CostingForm.tsx`/`CostingPreview.tsx`/`InvoicePreview.tsx` and `types/costing.ts`/`types/supabase.ts` thread `unit` through the line item shape end-to-end (costing → invoice snapshot). `lib/autocorrect.ts` also touched (unit-aware suggestion filtering). `npm run build` and `tsc --noEmit` pass.

* Feature (undocumented) — GST Report page (no spec written; reconstructed 2026-07-09): `app/(app)/gst-report/page.tsx` (Server Component, `force-dynamic` — reads `month`/`year` from searchParams, defaults to current month, queries `invoices` joined to `costings(client_name)` filtered by created_at range), `app/(app)/gst-report/GstReportClient.tsx` (client component — month/year picker via URL searchParams, 5 summary cards: Taxable Value/CGST/SGST/IGST/Total Tax, per-invoice breakdown table with totals footer, "Export CSV" button that builds and downloads a CSV client-side). Linked into sidebar nav (`components/layout/sidebar.tsx`, BarChart2 icon, "GST Report" between Items and Settings).

* Feature (undocumented) — Delete flows for clients/costings/invoices (no spec written; reconstructed 2026-07-09): `DELETE` handlers added to `app/api/clients/[id]/route.ts`, `app/api/costings/[id]/route.ts`, `app/api/invoices/[id]/route.ts` (auth check + ownership check scoped to `user_id`, 404 if not found/not owned). `app/(app)/clients/[id]/DeleteClientButton.tsx`, `app/(app)/costings/[id]/DeleteCostingButton.tsx`, `app/(app)/invoices/[id]/DeleteInvoiceButton.tsx` (client components — `window.confirm` guard, DELETE fetch, redirect to list view on success; invoice delete confirm text warns that payments cascade). Deleting an invoice does not touch the source costing (invoices.costing_id is nullable and set null on delete per `20260513092418_fix_invoices_costing_fk_on_delete_set_null.sql`, applied prior to this work).

* Feature (undocumented) — Edit Costing page (no spec written; reconstructed 2026-07-09): `app/(app)/costings/[id]/edit/page.tsx` (client component — fetches existing costing via `GET /api/costings/[id]` and business profile via `GET /api/business-profile`, renders the same two-column `CostingForm` + live `CostingPreview` layout as `/costings/new`, passing `initialState` and `costingId` so `CostingForm` PUTs instead of POSTs). Only reachable for costings, not invoices — invoices remain locked at creation per the no-edit-after-invoice business rule.

* Feature 14 — Bank Details & Terms and Conditions on Invoice: migration `20260708150000_add_bank_details_terms.sql` adds `bank_account_no`, `bank_ifsc`, `bank_name`, `terms_conditions` (not null, defaulted) to `business_profile`, applied to the live Supabase project via MCP. `types/supabase.ts` and `types/costing.ts` (`BusinessProfile`) updated. `app/api/business-profile/route.ts` GET/POST read and persist the new fields. `app/(app)/settings/SettingsForm.tsx` gained a "Bank Details" section (Bank Name, A/c No., IFSC) and a "Terms & Conditions" textarea seeded with the same default text as the DB column. `app/(app)/settings/page.tsx` and `app/(app)/invoices/[id]/page.tsx` select the new columns. `components/costing/InvoicePreview.tsx` renders a "Bank Details" row (only if any bank field is set) above the Terms & Conditions / signature grid, replaced the hardcoded jurisdiction text with `businessProfile.terms_conditions`, and added a "Receiver's Signature" line beside "For {business} / Authorised Signatory" — nothing is hardcoded anymore, all editable from Settings. `npx tsc --noEmit` passes. Not implemented: literal E-Invoice QR code (would require GST e-invoice/IRP integration, out of scope — not explicitly requested).

* Feature 13d — First-Run Business Profile Banner (13d-first-run-banner.md): components/shared/FirstRunBanner.tsx (client component — amber dismissible banner, checks sessionStorage on mount, hides immediately when show becomes false to handle async profile loading, dismiss stores 'bannerDismissed' in sessionStorage). Wired into costings/new/page.tsx (fetches profile via API, passes show condition), costings/[id]/page.tsx, and invoices/[id]/page.tsx (both server-side, businessProfile already fetched). `npm run build` passes.

* Feature 13c — HSN/SAC Item Master (13c-hsn-sac-item-master.md): ItemsManager already had the HSN/SAC column, add/edit inputs, and API routes already persisted hsn_sac. Added validation (/^\d{4,8}$/) in handleAdd() and saveEdit() — shows "Must be 4–8 digits" error and blocks submission on invalid non-empty input. Updated add-form placeholder to "HSN/SAC (optional)". `npm run build` passes.

* Feature 13b — Invoice Preview (13b-invoice-preview.md): components/costing/InvoicePreview.tsx — standalone component mirroring CostingPreview layout with heading "INVOICE", invoice number shown below heading, invoice date used; intra-state shows CGST+SGST rows, inter-state shows IGST row only; client GSTIN shown as "Buyer GSTIN"; legal footer added. app/(app)/invoices/[id]/page.tsx already wired correctly. `npm run build` passes with no TypeScript errors.

* Feature 13a — Edit Costing GST Fields Persistence (13a-edit-costing-gst.md): app/api/costings/[id]/route.ts PUT handler — added separate 404/403 ownership check (first select by id alone, then compare user_id), added 400 validation for invalid transaction_type, response now returns `{ costing }` full row instead of `{ data: { id } }`. GST fields (cgst/sgst/igst rate+amount, transaction_type) persist correctly via calculateTotals on every save; client_gstin cleared → null via `|| null` coercion.

* Feature 11 — Item Master Price List (11-item-master.md): app/(app)/items/page.tsx (fetches all items, passes to ItemsManager), app/(app)/items/ItemsManager.tsx (per-section "Add Item" button opens inline form row with Name/Rate inputs and Confirm button; inline edit of name/rate; active toggle with optimistic update; inactive items dimmed with strikethrough), app/api/items/route.ts (GET active items for autocomplete; POST new item), app/api/items/[id]/route.ts (PATCH name/default_rate/active scoped to user_id), components/costing/ItemRow.tsx (description field autocomplete dropdown filtered by section on focus; selecting fills description + rate), components/costing/ItemSection.tsx (passes section-filtered masterItems to each ItemRow), components/costing/CostingForm.tsx (fetches active items once on mount via GET /api/items, passes per-section to ItemSection).

* Feature 10 — Client Management (10-client-management.md): app/(app)/clients/page.tsx (fetches address + created_at from clients table), app/(app)/clients/ClientSearch.tsx (type updated; table now shows Name, Phone, Address, Costings count, Date Added columns), app/api/clients/[id]/route.ts (explicit ownership check — 404 if not found, 403 if wrong owner; returns `{ data: { id } }` instead of `{ ok: true }`). Detail page and ClientEditForm were already complete from Feature 07.

* Feature 09 — Payment Tracking (09-payment-tracking.md): app/api/payments/route.ts (POST — auth check, invoice ownership validation via user_id eq, inserts payment, recomputes invoice status inline by summing all payments vs grand_total — pending/partial/paid, returns `{ data: { id } }`), components/invoice/PaymentStages.tsx (client component — receives `invoice: { id, grand_total }` + `existingPayments`; three stage rows with labels "Advance (60%)"/"Pre-Delivery (30%)"/"Completion (10%)"; each shows expected amount, paid read-only view or input form with "Record Payment" button; on success calls router.refresh() for server-side data refresh; summary bar shows Total Received, Outstanding, overall status badge Pending/Partial/Paid), app/(app)/invoices/[id]/page.tsx (import updated to @/components/invoice/PaymentStages; props updated to `invoice={{ id, grand_total }}` and `existingPayments`; PaymentStages placed below InvoicePreview in print:hidden wrapper).

* Feature 12 — Dashboard (12-dashboard.md): app/(app)/dashboard/page.tsx (Server Component: three summary cards — Costings This Month, Invoiced This Month, Outstanding; Recent Costings table with "View All" link + "View" link column; DashboardSearch client component for search; all data server-side with force-dynamic), app/(app)/dashboard/DashboardSearch.tsx (client component: controlled input with useState, router.push on submit, clears results when input emptied, displays search results table with "View" links; receives initialQuery + initialResults as props from server). Also fixed MasterItemSuggestion type to include section field (ItemRow.tsx) — was causing build failure.

* Feature 08 — Item Master Price List (08-item-master.md): app/api/items/route.ts (GET — returns active items for costing form autocomplete; POST — creates item with section/name/default_rate), app/api/items/[id]/route.ts (PATCH — updates name/default_rate or toggles active, scoped to user_id), app/(app)/items/page.tsx (Server Component: fetches all items, passes to ItemsManager), app/(app)/items/ItemsManager.tsx (client component: grouped by section, inline edit of name/rate, toggle active with optimistic update, add new item form with section dropdown), components/costing/ItemRow.tsx (description field now shows autocomplete dropdown from masterItems for the section; selecting a suggestion fills description + rate), components/costing/ItemSection.tsx (accepts masterItems prop, passes section-filtered items to each ItemRow), components/costing/CostingForm.tsx (fetches active items once on mount via GET /api/items, passes filtered masterItems per section to ItemSection).

* Feature 08 (old) — Invoice Generation (08-invoice-generation.md): app/api/invoices/route.ts (POST — 201 with `{ data: { id, invoice_number } }` for new invoice; 400 with `{ error, data: { id } }` for existing — one invoice per costing), app/(app)/costings/[id]/ConvertToInvoiceButton.tsx (handles both 201 and 400 responses, reads data.id, navigates to existing invoice on second click), app/(app)/invoices/[id]/page.tsx (Back to Invoices link added to action bar), app/(app)/invoices/page.tsx (status badges now use design-system CSS vars: bg-status-paid/partial/pending instead of hardcoded Tailwind colors).

* Feature 07 (spec 07-costing-detail-list.md) — Costing Detail & List refinement: components/costing/PrintButton.tsx (shared client component — moved from per-page PrintButton files; used by both costing and invoice detail pages), app/(app)/costings/[id]/page.tsx (action bar updated: disabled Edit button removed, "Back to Costings" link with ArrowLeft icon added; PrintButton now imported from shared components/costing/PrintButton), app/(app)/costings/page.tsx (Actions column added with "View" link; dates now use formatDate from lib/format.ts), app/(app)/invoices/[id]/page.tsx (updated to import shared PrintButton). Print styles (A4 portrait, 14mm margins, sidebar/action bar hidden) were already in place from Feature 04.

* Feature 07 — Client Management (07-client-management.md): app/(app)/clients/page.tsx (Server Component: fetches all clients + costing counts via two queries, passes merged list to ClientSearch client component), app/(app)/clients/ClientSearch.tsx (client component: real-time filter by name or phone via useState, table with name/phone/costing count, links to detail), app/(app)/clients/[id]/page.tsx (Server Component: fetches client, linked costings via client_id, linked invoices via client_id, renders ClientEditForm + two linked-record tables with status badges), app/(app)/clients/[id]/ClientEditForm.tsx (client component: controlled inputs for name/phone/address/reference, PATCH fetch on save, inline saved/error feedback), app/api/clients/[id]/route.ts (PATCH — auth check, validates name, updates clients row scoped to user_id). Client auto-creation on costing save was already implemented in Feature 06 costing API.
* Feature 06 — Payment Tracking (06-payment-tracking.md): app/api/payments/route.ts (POST — auth check, inserts payment row with invoice_id/stage/amount/paid_at/notes), app/api/invoices/[id]/status/route.ts (PATCH — sums all payments for invoice, sets status to pending/partial/paid based on total vs grand_total), components/costing/PaymentStages.tsx (client component — three stage cards for advance/pre-delivery/completion showing expected %, amount input/date/notes fields, Mark as Paid button, read-only view after payment saved, outstanding balance header; POSTs payment then PATCHes status), app/(app)/invoices/[id]/page.tsx (fetches payments, passes to PaymentStages below InvoicePreview).
* Feature 06 — Costing API & Preview (06-costing-api-preview.md): app/api/costings/route.ts updated (client match by name via ilike — creates new clients row if no match, sets client_id on costing insert, returns `{ data: { id, costing_number } }`), components/costing/TotalsStrip.tsx updated (dark bg-gray-900 block), components/costing/CostingForm.tsx updated (onUpdate prop lifts CostingFormState to parent on every state change via useEffect, save redirect uses data.id from new response shape), app/(app)/costings/new/page.tsx rewritten as client component with two-column layout (60% form / 40% sticky CostingPreview live-updated on every keystroke).
* Feature 05 — Invoice Generation (05-invoice-generation.md): app/api/invoices/route.ts (POST — auth check, checks existing invoice for costing, generates INV-YYYY-NNN number, copies snapshot of line_items/totals from costing, inserts with status 'pending'), components/costing/InvoicePreview.tsx (same layout as CostingPreview, heading "INVOICE", shows invoice number), app/(app)/costings/[id]/ConvertToInvoiceButton.tsx (client component — POSTs to /api/invoices, redirects to /invoices/[id]), app/(app)/invoices/[id]/page.tsx (Server Component: fetches invoice + costing join for client fields, renders InvoicePreview + PrintButton), app/(app)/invoices/[id]/PrintButton.tsx (window.print()), app/(app)/invoices/page.tsx (list: invoice number, client name via costing join, total, date, payment status badge). Costing detail page updated to use ConvertToInvoiceButton instead of static link.
* Feature 05 — Costing Form (05-costing-form.md): components/costing/ItemRow.tsx (FormItem type, controlled inputs, formatINR on amount), components/costing/ItemSection.tsx (section + items + onAdd/onRemove/onChange props, SECTION_LABELS heading), components/costing/CostingForm.tsx (rewritten with useReducer, single-column layout, no preview panel, inline error display, save→redirect).
* Feature 04 (utilities spec 04-costing-utilities.md) — types/costing.ts (Section, LineItem, CostingFormState, Totals interfaces), lib/constants.ts (SECTIONS as simple string array, SECTION_LABELS, updated DEFAULT_TERMS), lib/costing.ts (generateCostingNumber, generateInvoiceNumber added; Totals no longer carries subtotal/freight), lib/format.ts (formatDate accepts string|Date). All consumers updated: TotalsStrip gets freight prop, CostingPreview uses formState.freight, detail page and API route fixed.
* Feature 04 — Costing Detail & PDF Export (04-costing-detail-pdf.md): app/(app)/costings/page.tsx (list: table of all costings sorted by date desc, status badge, link to detail), app/(app)/costings/[id]/page.tsx (Server Component: fetches costing from Supabase, maps DB row → CostingFormState + Totals, renders CostingPreview with static data, action bar with Edit/Convert to Invoice/Print buttons — print:hidden on action bar), app/(app)/costings/[id]/PrintButton.tsx (client component: window.print()). Sidebar already had print:hidden. Print button triggers browser print dialog.
* Feature 03 — Costing Form (03-costing-form.md): lib/constants.ts (SECTIONS, GST_RATE, DEFAULT_TERMS, prefixes), lib/costing.ts (calculateTotals — GST on kitchen only, freight without GST), lib/format.ts (formatINR, formatDate), app/api/costings/route.ts (POST — auth check, server-side totals, LI-YYYY-NNN number generation, Supabase insert), components/costing/TotalsStrip.tsx, components/costing/CostingPreview.tsx (live print-document preview), components/costing/CostingForm.tsx (two-column layout, useState row management, save + redirect), app/(app)/costings/new/page.tsx.
* Feature 02 — App Shell & Auth (02-app-shell-auth.md): lib/supabase/client.ts (browser client), lib/supabase/server.ts (server client), proxy.ts (session refresh + auth redirects — Next.js 16 uses proxy.ts not middleware.ts), app/(auth)/layout.tsx, app/(auth)/login/page.tsx, components/layout/sidebar.tsx (dark sidebar, Lucide icons, active route highlight, sign out), app/(app)/layout.tsx (240px fixed sidebar shell), app/(app)/dashboard/page.tsx (placeholder). `npm run build` passes.
  * Note: .env.local keys exist but Supabase URL/anon key values are empty — fill before testing auth
* Feature 01 — Design System (01-design-system.md): deps installed, shadcn/ui initialised (Tailwind v4), all 10 components added, globals.css design tokens defined, lib/utils.ts cn() helper, .env.local created
* Feature 01 (old spec 01-project-setup-auth.md) — project setup portion: Next.js scaffolded, all deps installed (@supabase/supabase-js, @supabase/ssr, lucide-react, zod), .env.local keys present, CSS color tokens in globals.css
* Feature 03 (database schema spec 03-database-schema.md) — migration file saved (supabase/migrations/001_initial_schema.sql), applied to Supabase: 5 tables (clients, costings, invoices, item_master, payments) with RLS (`FOR ALL` policies scoped to `auth.uid()`), TypeScript types regenerated (types/supabase.ts). Key schema details: `costings.costing_number` UNIQUE, `costings.status` default `'saved'`, `gst_rate` default 18, `invoices.costing_id` NOT NULL, `payments.paid_at` is `date` type, `item_master.section` has CHECK constraint.

## In Progress

* None.

## Next Up

* None — all known work (13x specs + Features 14-15 + retroactively documented GST Report/delete/edit-costing) is shipped and builds clean.

## Open Questions

* None yet.

## Architecture Decisions

* Stack: Next.js 14 (App Router) + Supabase + Tailwind + shadcn/ui
* Line items stored as JSONB snapshot on costing row — not normalised
* Costing number format: `LI-YYYY-NNN` (e.g. LI-2026-001), resets each year — invoice format: `INV-YYYY-NNN`
* Single owner login — no multi-user, no roles

## Session Notes

* Node.js required: v18+
* Next.js version: 14 (use `npx create-next-app@latest`)
* Supabase JS client: `@supabase/supabase-js` + `@supabase/ssr`
* shadcn/ui: install via `npx shadcn-ui@latest init` after project creation
